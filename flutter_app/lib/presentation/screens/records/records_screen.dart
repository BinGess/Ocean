/// 碎片记录页面
/// 显示所有快速记录

import 'package:flutter/material.dart';

class RecordsScreen extends StatelessWidget {
  const RecordsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('碎片记录'),
      ),
      body: const Center(
        child: Text('碎片记录页面 - 待实现'),
      ),
    );
  }
}
