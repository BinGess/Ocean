/// 周洞察页面
/// 显示情绪模式和微实验

import 'package:flutter/material.dart';

class InsightsScreen extends StatelessWidget {
  const InsightsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('周洞察'),
      ),
      body: const Center(
        child: Text('周洞察页面 - 待实现'),
      ),
    );
  }
}
